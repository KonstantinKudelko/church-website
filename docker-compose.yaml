version: "3.8"

services:
  db:
    image: postgres:12
    container_name: website-db-dev
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=websitedb
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
    networks:
      - backend

  server:
    build:
      context: ./server
      dockerfile: Dockerfile.dev
    image: website-server:dev
    container_name: website-server-dev
    environment:
      - DATABASE_CLIENT=postgres
      - DATABASE_HOST=db
      - DATABASE_PORT=5432
      - DATABASE_NAME=websitedb
      - DATABASE_USERNAME=root
      - DATABASE_PASSWORD=root
    volumes:
      - /app/node_modules
      - ./server:/app
    networks:
      - backend

  client:
    build:
      context: ./client
      dockerfile: Dockerfile.dev
    image: website-client:dev
    container_name: website-client-dev
    volumes:
      - /app/node_modules
      - ./client:/app
    networks:
      - frontend

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile.dev
    image: website-router:dev
    container_name: website-router-dev
    restart: unless-stopped
    ports:
      - 8080:80
    networks:
      - backend
      - frontend

volumes:
  db-data:
    name: website-db-dev-data

networks:
  frontend:
    name: website-network-frontend
    driver: bridge
  backend:
    name: website-network-backend
    driver: bridge
